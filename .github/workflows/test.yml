name: test

on:
  pull_request:
    branches: [main]

jobs:
  cypress-run:
    runs-on: ubuntu-latest
    environment: test
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: add env
        env:
          MONGODB_URI: ${{ secrets.MONGODB_URI }}
          JWT_SECRET: ${{ secrets.JWT_SECRET }}
        working-directory: ./www
        run: echo "The API key is:${{env.MONGODB_URI}}"
      - name: Cypress run
        uses: cypress-io/github-action@v2
        with:
          working-directory: ./www
          build: npm run build
          start: npm start
